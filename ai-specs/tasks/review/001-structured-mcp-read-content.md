# Task: Improve MCP subtree read outputs for agents
- **Task Identifier:** 2026-07-05-structured-read
- **Scope:** Improve MCP subtree read output for LLM/agent use. This
  task covers structured JSON node content from
  `readNodesWithDescendants` and compact agent-oriented plain text from
  `readNodesWithDescendantsAsPlainText`. Do not change map lookup,
  node selection, traversal depth, context-section request fields, or
  editing tools except where read DTO changes require compilation
  updates.
- **Motivation:** The subtree read tools feed LLM/agent context. JSON
  responses need structured fields instead of presentation labels.
  Plain-text responses need compact, useful context rather than empty
  bullets or redundant labels that spend tokens without helping answer
  the user's question.
- **Scenario:** An MCP client reads a selected subtree. JSON output
  exposes structured node `content`. Plain-text output preserves the
  actual Freeplane node topology, uses node text as the bullet title,
  labels only secondary fields such as details and attributes, omits
  irrelevant ordinary empty leaf nodes, and renders summary structure
  nodes as semantic markers.
- **Constraints:**
  - Preserve the existing request contracts for both subtree read tools.
  - Preserve the structured JSON response contract from the completed
    JSON subtask, including `content` and no `unformattedText` field.
  - Preserve `readNodesWithDescendantsAsPlainText` as the compact text
    surface for LLM/agent context.
  - Do not add compatibility duplicates for removed or changed display
    fields.
  - Keep string content plain text, not HTML.
  - Do not change `fetchNodesForEditing`; raw editable text remains
    available there.
- **Briefing:** The MCP read tools are exposed through `AIToolSet` in
  `freeplane_plugin_ai`. `ReadNodesWithDescendantsTool` owns both the
  structured JSON subtree read and the compact plain-text subtree read.
  `NodeDepthItem` carries per-node read content and optional context
  sections such as qualifiers, hyperlinks, connectors, and clone
  metadata. `TextController.getShortPlainText(NodeModel)` already
  shortens summary text to 40 characters with continuation mark ` ...`.
- **Analysis:**
  - The plain-text read tool is an LLM/agent context surface, not a
    human-display surface.
  - Empty plain-text bullets are not useful context when they represent
    ordinary empty leaf nodes with no requested metadata.
  - The primary node text should be the bullet title; a leading
    `Text:` label is redundant when the bullet already denotes the
    node.
  - Secondary fields still need labels because otherwise details,
    notes, attributes, tags, icons, and metadata lose field meaning.
  - Summary structure nodes are part of the actual Freeplane topology
    and must be rendered as structural markers in plain text even when
    `QUALIFIERS` is not requested.

## Subtask: Return structured JSON content from subtree reads
- **Status:** review
- **Scope:** Rework `readNodesWithDescendants` so its JSON response
  returns structured node content instead of presentation-formatted
  `unformattedText`. Keep `readNodesWithDescendantsAsPlainText` as the
  compact labeled text surface. Do not change node selection, depth,
  context-section, map lookup, or editing tools except where they need
  to compile against the new response DTO.
- **Motivation:** The current JSON response can return
  `"unformattedText": "Text: example summary"` for a node whose raw
  text is `example summary`. That prefix is generated by the AI plugin,
  not by Freeplane core. In JSON this loses field boundaries, makes
  client parsing ambiguous, and misrepresents a display rendering as raw
  node text.
- **Scenario:** An MCP client reads a selected subtree. For nodes inside
  `fullContentDepth`, the JSON response exposes separate plain-text
  `text`, `details`, `note`, `attributes`, `tags`, and `icons` fields.
  For nodes outside `fullContentDepth`, the JSON response exposes only a
  `shortText` field. A client that wants display-oriented text calls
  `readNodesWithDescendantsAsPlainText` and receives the existing
  labeled tree rendering.
- **Constraints:**
  - Preserve the `readNodesWithDescendants` request contract.
  - Preserve the response envelope fields `mapIdentifier`, `items`,
    `nodes`, `omissions`, `parentNode`, and `breadcrumbPath` where they
    remain semantically valid.
  - Remove the JSON `unformattedText` node field instead of keeping a
    compatibility duplicate.
  - Use `content` as the JSON property for structured node content; do
    not use `unformattedContent`.
  - Preserve `readNodesWithDescendantsAsPlainText` output semantics,
    including labels such as `Text:` and `Details:` for full-content
    plain-text rendering.
  - Keep null fields omitted through the existing Jackson non-null DTO
    convention. Do not synthesize empty arrays for absent content.
  - Structured JSON content returned by `readNodesWithDescendants`
    remains plain text, not HTML. If a source field is HTML, convert it
    to plain text before assigning it to the node `content` field.
  - Do not change `fetchNodesForEditing`; raw editable text remains
    available there.
- **Briefing:** The MCP tools are exposed through `AIToolSet` in
  `freeplane_plugin_ai`. `ReadNodesWithDescendantsTool` owns the JSON
  subtree read, the plain-text subtree read, focus-node resolution,
  traversal, budget accounting, and read-tool summaries.
  `NodeDepthItem` is the per-node JSON DTO used by both subtree read
  variants. `NodeContentResponse` and its nested `*Content` DTOs are
  internal content-reader DTOs already used by other read/edit paths;
  they should not define the external JSON shape for this tool.
- **Research:**

  ```plantuml
  @startuml
  set separator none
  package "Freeplane AI" {
    package "org.freeplane.plugin.ai.tools" {
      package read {
        class ReadNodesWithDescendantsTool {
          - buildNodeDepthItem(...)
          - buildUnformattedText(NodeContentResponse)
          - appendLabeledLine(List<String>, String, String)
          - renderNodeContribution(NodeDepthItem, int)
        }
        class NodeDepthItem {
          - String nodeIdentifier
          - int depth
          - String unformattedText
          - List<String> qualifiers
          - String hyperlink
          - List<ConnectorItem> outgoingConnectors
          - List<ConnectorItem> incomingConnectors
          - CloneMetadata cloneMetadata
          + getUnformattedText()
        }
      }
      package content {
        class NodeContentItemReader {
          + readNodeContent(NodeModel, NodeContentRequest, NodeContentPreset)
        }
        class NodeContentResponse {
          - String briefText
          - TextualContent textualContent
          - AttributesContent attributesContent
          - TagsContent tagsContent
          - IconsContent iconsContent
          - List<String> activeStyles
          - String mainStyle
          - EditableContent editableContent
        }
        class TextualContent {
          - String text
          - String details
          - String note
        }
        class AttributesContent {
          - List<AttributeEntry> attributes
        }
        class TagsContent {
          - List<String> tags
        }
        class IconsContent {
          - List<String> descriptions
        }
        class AttributeEntry
        class ConnectorItem
        class CloneMetadata
        class EditableContent
      }
    }
  }
  ReadNodesWithDescendantsTool --> NodeContentItemReader
  ReadNodesWithDescendantsTool ..> NodeDepthItem
  ReadNodesWithDescendantsTool ..> NodeContentResponse
  NodeDepthItem --> ConnectorItem
  NodeDepthItem --> CloneMetadata
  NodeContentResponse --> TextualContent
  NodeContentResponse --> AttributesContent
  NodeContentResponse --> TagsContent
  NodeContentResponse --> IconsContent
  NodeContentResponse --> EditableContent
  AttributesContent --> AttributeEntry
  @enduml
  ```

  - `ReadNodesWithDescendantsRequest` defaults `fullContentDepth` to
    `0`, so the focus node is read as full content by default.
  - `ReadNodesWithDescendantsTool.buildNodeDepthItem(...)` currently
    reads full content for nodes whose depth is within
    `fullContentDepth`; nodes beyond that depth use
    `TextController.getShortPlainText(...)`.
  - `TextController.getShortPlainText(NodeModel)` returns transformed
    plain node text shortened to 40 characters with continuation mark
    ` ...`, with node numbering disabled.
  - `TextualContentReader` converts full text, details, and note values
    to plain text with `HtmlUtils.htmlToPlain(...)` after applying
    Freeplane text transformations.
  - `NodeContentResponse.briefText` is an internal name for the short
    plain node text. Existing MCP selection responses use `shortText`,
    which is clearer for the external subtree JSON contract.
  - `buildUnformattedText(...)` turns structured `NodeContentResponse`
    fields into display lines by calling `appendLabeledLine(...)` with
    labels `Text`, `Details`, `Note`, `Attributes`, `Tags`, and
    `Icons`.
  - `appendLabeledLine(...)` creates the visible prefix with
    `label + ": " + value`.
  - Current attribute values are converted to plain text inside
    `buildUnformattedText(...)` with `HtmlUtils.htmlToPlain(...)`, so
    the structured JSON path needs to preserve that conversion instead
    of exposing formatted attribute markup.
  - `ReadNodesWithDescendantsToolTest` asserts current JSON values such
    as `Text: Focus full`, proving the label is part of the tool
    response contract rather than Freeplane node content.
  - `readNodesWithDescendantsAsPlainText` renders
    `NodeDepthItem.unformattedText` into the compact bullet tree, where
    the labels are appropriate presentation text.
  - Reusing `NodeContentResponse` directly as JSON content would expose
    internal wrapper fields such as `textualContent`,
    `attributesContent`, `tagsContent`, and `iconsContent` where the
    external response only needs flat content fields.
- **Analysis:**
  - `readNodesWithDescendants` returns structured content because JSON
    clients need field boundaries without parsing presentation labels.
  - `readNodesWithDescendantsAsPlainText` keeps labeled rendering so
    that display-oriented consumers still get compact readable text.
  - `NodeDepthItem` replaces `unformattedText` with `content` because
    keeping both fields would preserve the ambiguous JSON contract.
  - The `content` field uses a flat read-specific DTO because the
    `*Content` wrappers are internal reader/editor structure, not a
    useful external MCP response shape.
  - Structured content fields remain plain text because the existing
    subtree read contract exposes a plain rendering, not raw HTML.
  - Nodes outside `fullContentDepth` use `content.shortText` because
    the existing depth contract intentionally exposes shortened
    summary-only content for those nodes.
  - The conversion from internal content DTOs to flat JSON content
    happens at the read-tool boundary so shared content-reader and
    edit-tool DTOs do not absorb MCP response-shape concerns.
  - Attribute values need explicit plain-text normalization in the
    structured read path because the current label renderer owns that
    conversion.
- **Design:**

  ```plantuml
  @startuml
  set separator none
  package "Freeplane AI" {
    package "org.freeplane.plugin.ai.tools" {
      package read {
        class ReadNodesWithDescendantsTool {
          - buildNodeDepthItem(...)
          - renderPlainTextContent(ReadNodeContent)
          - renderNodeContribution(NodeDepthItem, int)
        }
        class NodeDepthItem {
          - String nodeIdentifier
          - int depth
          - ReadNodeContent content
          - List<String> qualifiers
          - String hyperlink
          - List<ConnectorItem> outgoingConnectors
          - List<ConnectorItem> incomingConnectors
          - CloneMetadata cloneMetadata
          + getContent()
        }
        class ReadNodeContent {
          - String shortText
          - String text
          - String details
          - String note
          - List<AttributeEntry> attributes
          - List<String> tags
          - List<String> icons
          + getShortText()
          + getText()
          + getDetails()
          + getNote()
          + getAttributes()
          + getTags()
          + getIcons()
        }
        class ReadNodeContentMapper {
          {static} + fromFullContent(NodeContentResponse)
          {static} + fromShortText(String)
        }
      }
      package content {
        class NodeContentItemReader {
          + readNodeContent(NodeModel, NodeContentRequest, NodeContentPreset)
        }
        class NodeContentResponse {
          - String briefText
          - TextualContent textualContent
          - AttributesContent attributesContent
          - TagsContent tagsContent
          - IconsContent iconsContent
          - List<String> activeStyles
          - String mainStyle
          - EditableContent editableContent
        }
        class TextualContent {
          - String text
          - String details
          - String note
        }
        class AttributesContent {
          - List<AttributeEntry> attributes
        }
        class TagsContent {
          - List<String> tags
        }
        class IconsContent {
          - List<String> descriptions
        }
        class AttributeEntry
        class ConnectorItem
        class CloneMetadata
        class EditableContent
      }
    }
  }
  ReadNodesWithDescendantsTool --> NodeContentItemReader
  ReadNodesWithDescendantsTool ..> NodeDepthItem
  ReadNodesWithDescendantsTool ..> ReadNodeContentMapper
  ReadNodesWithDescendantsTool ..> ReadNodeContent
  ReadNodeContentMapper ..> NodeContentResponse
  ReadNodeContentMapper ..> ReadNodeContent
  NodeDepthItem --> ReadNodeContent
  NodeDepthItem --> ConnectorItem
  NodeDepthItem --> CloneMetadata
  ReadNodeContent --> AttributeEntry
  NodeContentResponse --> TextualContent
  NodeContentResponse --> AttributesContent
  NodeContentResponse --> TagsContent
  NodeContentResponse --> IconsContent
  NodeContentResponse --> EditableContent
  AttributesContent --> AttributeEntry
  @enduml
  ```

  ```plantuml
  @startuml
  actor "MCP client" as Client
  participant "AIToolSet" as ToolSet
  participant "ReadNodesWithDescendantsTool" as ReadTool
  participant "NodeContentItemReader" as ContentReader
  participant "ReadNodeContentMapper" as Mapper
  participant "TextController" as TextController

  Client -> ToolSet: readNodesWithDescendants(request)
  ToolSet -> ReadTool: readNodesWithDescendants(request)
  ReadTool -> ReadTool: resolve map, focus nodes, and traversal depth
  alt depth <= fullContentDepth
    ReadTool -> ContentReader: readNodeContent(node, FULL_CONTENT_REQUEST, FULL)
    ContentReader --> ReadTool: NodeContentResponse
    ReadTool -> Mapper: fromFullContent(internalContent)
    Mapper --> ReadTool: ReadNodeContent with flat plain fields
  else depth > fullContentDepth
    ReadTool -> TextController: getShortPlainText(node)
    TextController --> ReadTool: short text
    ReadTool -> Mapper: fromShortText(shortText)
    Mapper --> ReadTool: ReadNodeContent with shortText
  end
  ReadTool --> ToolSet: response with NodeDepthItem.content
  ToolSet --> Client: JSON response
  @enduml
  ```

  - `NodeDepthItem` constructor and JSON properties change from
    `unformattedText` to `content`.
  - `ReadNodeContent` is a read-tool-specific DTO in
    `org.freeplane.plugin.ai.tools.read`. It is annotated to omit null
    fields and described as structured node content whose string values
    are plain text, not HTML.
  - `ReadNodeContent` fields are `shortText`, `text`, `details`,
    `note`, `attributes`, `tags`, and `icons`.
  - `ReadNodeContentMapper` is package-private in
    `org.freeplane.plugin.ai.tools.read`. It is the only place that
    converts and flattens internal `NodeContentResponse` data for this
    response.
  - `ReadNodeContentMapper.fromFullContent(NodeContentResponse)` maps:
    - `TextualContent.text` -> `ReadNodeContent.text`.
    - `TextualContent.details` -> `ReadNodeContent.details`.
    - `TextualContent.note` -> `ReadNodeContent.note`.
    - `AttributesContent.attributes` -> `ReadNodeContent.attributes`.
    - `TagsContent.tags` -> `ReadNodeContent.tags`.
    - `IconsContent.descriptions` -> `ReadNodeContent.icons`.
  - `ReadNodeContentMapper.fromFullContent(...)` ignores
    `NodeContentResponse.briefText`, `activeStyles`, `mainStyle`, and
    `editableContent` for this tool.
  - `ReadNodeContentMapper.fromShortText(String)` sets only
    `ReadNodeContent.shortText`.
  - The mapper normalizes attribute values with
    `HtmlUtils.htmlToPlain(...)` before assigning them to
    `ReadNodeContent.attributes[*].value`.
  - The mapper preserves the existing icon strings from
    `IconsContent.descriptions` but exposes them under the external
    field name `icons`.
  - A full-content node whose actual text starts with `Text: ` returns
    that exact value as `content.text`; the tool adds no second `Text:`
    prefix in JSON.
  - Full-content node example:

    ```json
    {
      "nodeIdentifier": "ID_focus",
      "depth": 0,
      "content": {
        "text": "example summary",
        "details": "details value",
        "note": "note value",
        "attributes": [
          { "name": "key", "value": "value" }
        ],
        "tags": ["tag"],
        "icons": ["button_ok"]
      }
    }
    ```

  - Summary-only node example:

    ```json
    {
      "nodeIdentifier": "ID_child",
      "depth": 1,
      "content": {
        "shortText": "Child 1"
      }
    }
    ```

  - `readNodesWithDescendantsAsPlainText` renders
    `NodeDepthItem.content` through `renderPlainTextContent(...)`.
    Full-content nodes still render labeled lines such as
    `Text: value`, `Details: value`, and `Attributes: key=value`.
    Summary-only nodes render only `content.shortText`.
  - JSON response budget accounting continues to measure serialized
    JSON response size after the DTO change. Plain-text response budget
    accounting remains based on rendered character count.
  - Tool summaries keep using focus-node preview text and do not expose
    the removed `unformattedText` field.
- **Test specification:**
  - **Automated tests:**
    - `ReadNodesWithDescendantsToolTest`
      - `readNodesWithDescendants_returnsFlatContentForFullAndShortNodes`:
        the focus node returns flat full content and children beyond
        `fullContentDepth` return `content.shortText`.
      - `readNodesWithDescendants_returnsFullContentWithinFullContentDepth`:
        descendants inside `fullContentDepth` return flat full content
        instead of labeled `unformattedText`.
      - `readNodesWithDescendants_preservesTextPrefixAsNodeText`:
        a node whose actual text starts with `Text: ` returns that
        value exactly in `content.text` without adding a presentation
        prefix.
      - `readNodesWithDescendants_returnsFlatNonTextFields`: details,
        note, attributes, tags, and icons remain separate JSON fields
        without `*Content` wrappers.
      - `readNodesWithDescendants_returnsPlainStructuredContentNotHtml`:
        formatted text, details, note, and attribute values are returned
        as plain text in structured JSON fields.
      - `readNodesWithDescendantsAsPlainText_rendersStructuredContentWithLabels`:
        plain-text output still uses labels for full-content nodes and
        short text for summary-only nodes.
      - `readNodesWithDescendantsAsPlainText_countsRenderedCharactersInsteadOfJsonSize`:
        plain-text budget behavior is unchanged after `NodeDepthItem`
        stores structured content.
      - `readNodesWithDescendants_omitsAdditionalFocusNodesWhenBudgetExceeded`:
        serialized JSON budget omission behavior still applies to the
        changed DTO.
    - `ReadNodeContentMapperTest`
      - `fromFullContent_flattensPlainFields`: wrapper DTO fields map
        to flat `ReadNodeContent` fields.
      - `fromFullContent_normalizesAttributeValuesToPlainText`: HTML
        attribute values are plain text in the external read content.
      - `fromShortText_setsOnlyShortText`: summary-only content exposes
        `shortText` and no full-content fields.
    - `AIToolSetToolExposureTest`
      - `readNodesWithDescendants_isExposedAsToolMethod`: the tool
        remains exposed after the response DTO change.

## Subtask: Optimize plain-text subtree rendering for agent context
- **Status:** review
- **Scope:** Rework only `readNodesWithDescendantsAsPlainText` rendering
  so its compact text output is more useful as LLM/agent context. Keep
  the structured JSON behavior from the completed subtask unchanged.
  Keep the plain-text response envelope and budget mechanism, but allow
  the rendered text format to change.
- **Motivation:** Current plain-text output can render `- Text: value`
  for ordinary node text and `-` or `-  [summary_node]` for hidden
  summary-structure nodes. For agents, those forms spend tokens and
  obscure the actual Freeplane topology needed to answer the user's
  question.
- **Scenario:** For a selected subtree whose actual topology contains
  summary group-start nodes and summary nodes, default plain-text output
  preserves that topology with structural markers:

  ```text
  - example summary
    - [summarized]
    - adfsdf
      - sdfsdf
      - sdfsdfaf
    - [summary]
      - some SUMMARY content
    - [summarized]
    - 1
      - 2
    - [summary]
      - some other Summary content
  ```

  Rich full-content nodes keep labels for secondary fields:

  ```text
  - Release plan
    Details: Prepare version 1.2 release.
    Attributes: owner=Alice; status=draft
    Tags: planning, release
  ```
- **Constraints:**
  - Do not change `readNodesWithDescendants` JSON output.
  - Do not change request parameters or add a new plain-text mode.
  - Preserve actual Freeplane node topology in plain text: do not
    promote children, reattach summary content, or infer structure from
    visual layout.
  - Render summary structure markers even when `QUALIFIERS` is not
    requested.
  - Render `summary_node` as `[summary]` and keep its child nodes under
    that marker.
  - Render `first_group_node` as `[summarized]`.
  - Omit ordinary empty leaf nodes with no rendered metadata.
  - Render ordinary empty non-leaf nodes as `[untitled]`.
  - Do not report semantically skipped ordinary empty leaves as budget
    omissions. Budget omissions remain about text budget exhaustion.
  - Preserve existing labels for secondary content and metadata lines.
  - Preserve existing short-text truncation behavior and continuation
    mark from `TextController.getShortPlainText(NodeModel)`.
- **Briefing:** Current plain-text rendering is in
  `ReadNodesWithDescendantsTool.renderFocusNodeAsPlainText(...)` and
  `renderNodeContribution(...)`. Nodes are held as a preorder flat
  `List<NodeDepthItem>` with `depth`. `SummaryNode.isSummaryNode(...)`
  and `SummaryNode.isFirstGroupNode(...)` are exposed in read DTOs as
  qualifiers `summary_node` and `first_group_node`. Plain-text output
  must use those flags as structural markers independent of whether the
  caller requested `ContextSection.QUALIFIERS`.
- **Research:**
  - `SummaryNode.isHidden(NodeModel)` treats unfolded empty summary and
    first-group nodes as hidden structural nodes.
  - `summary_node` is the bracket-center/control node. It is not
    rendered as content by Freeplane, but its children are the visible
    summary content.
  - `first_group_node` marks the start of the summarized group.
  - Live MCP output before this subtask exposed empty bullets and raw
    qualifier names for those structural nodes.
  - `TextController.getShortPlainText(NodeModel)` delegates to
    `getShortPlainText(nodeModel, 40, " ...")`, so summary-only node
    truncation already has an explicit continuation mark.
- **Analysis:**
  - A plain-text node line should first communicate the node's useful
    content. For a node with text, the text itself is the useful title;
    `Text:` adds no field boundary the bullet does not already provide.
  - Secondary fields require labels because they are not implied by the
    bullet marker.
  - Summary structure is topology, not optional display decoration, for
    this agent-context surface.
  - Because the whole node structure is available, the renderer should
    not infer or rewrite hierarchy from visual layout.
  - Ordinary empty leaf nodes with no rendered content and no rendered
    metadata can be skipped because they carry no child topology.
  - Ordinary empty non-leaf nodes must remain visible as `[untitled]`
    so descendants are not promoted.
  - Full-content node text is not shortened by this tool. If the text
    budget is exhausted, the existing omission mechanism omits later
    rendered nodes rather than partially rendering a full-content field.
- **Design:**
  - Change plain-text rendering only. `NodeDepthItem.content` remains
    the shared input for both read variants.
  - Plain-text reads always request qualifier flags internally so
    summary structure can be rendered even without requested
    `QUALIFIERS`.
  - Use ordered render lines:
    - primary title line: `shortText` or `text` without a `Text:` label;
    - secondary content lines: labeled `Details:`, `Note:`,
      `Attributes:`, `Tags:`, and `Icons:`;
    - metadata lines: existing labeled hyperlink, connector, and clone
      metadata lines.
  - Render the first available line on the bullet line. Render
    remaining lines indented under the bullet.
  - Convert qualifier names to compact semantic markers for plain text:
    - `summary_node` -> `[summary]`;
    - `first_group_node` -> `[summarized]`.
  - If a node has a primary title and structural markers, append the
    markers after the title on the same bullet line.
  - If a node has no title or secondary lines but has structural
    markers, use the marker text as the bullet title.
  - Skip a node in plain-text output only when all of these are true:
    - it has no primary title;
    - it has no secondary content lines;
    - it has no rendered metadata lines;
    - it has no structural markers; and
    - it is a leaf in the rendered preorder range.
  - If an otherwise empty ordinary node has rendered descendants, render
    `[untitled]` to preserve topology.
  - Determine whether a node has rendered descendants from the flat
    preorder list: the next node with greater depth means the current
    node has descendants in the rendered range.
  - Skipped ordinary empty leaves do not increment `renderedNodeCount`,
    do not consume text budget, and do not affect budget omission
    counts.
  - Keep short-text ellipsis behavior by continuing to use
    `TextController.getShortPlainText(NodeModel)` for summary-only
    nodes and by preserving returned short text exactly.
- **Test specification:**
  - **Automated tests:**
    - `ReadNodesWithDescendantsToolTest`
      - `readNodesWithDescendantsAsPlainText_rendersPrimaryTextWithoutTextLabel`:
        full-content and summary-only node text render as bullet titles
        without a leading `Text:` label.
      - `readNodesWithDescendantsAsPlainText_keepsSecondaryFieldLabels`:
        details, note, attributes, tags, icons, hyperlinks, connectors,
        and clone metadata remain labeled continuation lines.
      - `readNodesWithDescendantsAsPlainText_omitsEmptyLeafNodesWithoutMetadata`:
        ordinary empty leaf nodes with no requested metadata are skipped
        and are not reported as budget omissions.
      - `readNodesWithDescendantsAsPlainText_rendersSummaryMarkersByDefaultAndPreservesTopology`:
        `summary_node` renders as `[summary]`, `first_group_node`
        renders as `[summarized]`, and summary content remains
        indented under the `[summary]` marker.
      - `readNodesWithDescendantsAsPlainText_preservesUntitledBranches`:
        an otherwise empty ordinary node with rendered descendants is
        kept as `[untitled]` so indentation remains truthful.
      - `readNodesWithDescendantsAsPlainText_preservesShortTextContinuationMark`:
        shortened summary text returned by `TextController` is rendered
        unchanged, including ` ...`.
      - `readNodesWithDescendantsAsPlainText_countsRenderedCharactersAfterSkippingEmptyLeaves`:
        plain-text budget accounting measures the final rendered text
        after semantic empty-leaf skipping.
- **Implementation notes:**
  - Implemented plain-text rendering with ordered content, metadata,
    and structural marker lines.
  - Primary node text now renders as the bullet title. Secondary
    content and metadata keep labels.
  - Plain-text reads always include summary qualifiers internally so
    summary topology is represented without requiring `QUALIFIERS` in
    the request.
  - Ordinary empty leaves without rendered content are skipped before
    budget counting. Ordinary empty branches with rendered descendants
    use `[untitled]` to preserve topology.
  - Summary structure markers render as `[summary]` and
    `[summarized]` in plain text.
