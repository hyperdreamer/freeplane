package org.freeplane.plugin.graph.control;

import org.freeplane.plugin.graph.projection.GraphProjection;

public interface GraphProjectionListener {
    void onGraphProjection(GraphProjection projection);
}
